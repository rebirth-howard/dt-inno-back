spring:
  config:
    import:
      - 'classpath:persistence-property.yml'

debug: false

logging:
  level:
    com.deutschmotors : debug
    org.springframework.web.servlet: debug
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    org.flywaydb: info
    security=DEBUG: